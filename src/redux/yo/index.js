import initialState from 'app/redux/store/initialState'
import {} from './types'

export default function (state = initialState.yo, action) {
  switch (action.type) {
    default:
      return state
  }
}
